{
  "name": "planner-pwa-static",
  "version": "3.0.0",
  "private": true,
  "description": "Static server for Planner PWA (serves /app)",
  "scripts": {
    "start": "serve -s .. -l tcp://0.0.0.0:${PORT:-3000} --no-clipboard --single"
  },
  "dependencies": {
    "serve": "^14.2.3"
  }
}
